import React from 'react';
import classes from './HomeView.scss';

export const HomeView = () => (
  <div>
    <h4 className={classes.title}>Welcome to our Site!</h4>
  </div>
);

export default HomeView;
